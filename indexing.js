/**
 *   
 */

var name = "chirag"//string store like a char array 
	
	console.log(name.charAt(2)); //charAt()function is using for position of String or char Array

var newName = name.concat("Patel"); // Concatinating to one string to other string
console.log(newName)

console.log(newName.indexOf("P")) // IndexOf is for Indexing

console.log(newName.slice(1,7)) // 1st index is inclusive , last index is exclusive

console.log(newName.toUpperCase())//toUpperCase is a function for Upper case letter


var n = "chirag ";
console.log(n.trim()) //if any string have space at the end , than we can use Trim Method

